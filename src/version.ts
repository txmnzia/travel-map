/**
 * User-visible app version, shown at the bottom of the map-style picker.
 * Format: vYYYYMMDD-N — N resets to 1 each day and increments for each
 * additional user-facing change on the same day. Bump this in every commit
 * that changes user-facing code (see CLAUDE.md).
 */
export const VERSION = 'v20260703-5';
